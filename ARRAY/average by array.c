#include<stdio.h>
int main()
{
	int a[4],i;
	for(i=0;i<=9;i++)
	 {
	
	 a[i]=i;
	 printf("%u\n",a*[i]);
    }
/*	int i,mark[10],sum=0;
	float avg;
	for(i=0;i<=9;i++)
	{
		printf("Enter marks :");
		scanf("%d",& mark[i]);
		
	}
	for(i=0;i<=9;i++)
	  sum=sum+mark[i];
	  avg=sum/10.0;
	  printf("average marks = %f",avg);
	  */return 0;
	
}